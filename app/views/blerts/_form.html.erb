<%= form_for(@blert) do |f| %>
  <% if @blert.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@blert.errors.count, "error") %> prohibited this alert from being saved:</h2>

      <ul>
      <% @blert.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :alerttype %><br />
    <%= f.text_field :alerttype %>
  </div>
  <div class="field">
    <%= f.label :contact %><br />
    <%= f.text_field :contact %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>

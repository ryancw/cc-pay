<%= form_for(@blert) do |f| %>
  <% if @blert.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@blert.errors.count, "error") %> prohibited this alert from being saved:</h2>

      <ul>
      <% @blert.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  
  <div class="field">
    <%= f.select :contact, Blert::CONTACT %>
  </div>
  <div class="field">
    <%= f.select :alerttype, Blert::TYPE %>
  </div>
  
  <div class="actions">
    <%= f.submit "Create Alert" %>
  </div>
<% end %>
